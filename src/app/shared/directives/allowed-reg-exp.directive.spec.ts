import { AllowedRegExpDirective } from './allowed-reg-exp.directive';

describe('AllowedRegExpDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowedRegExpDirective();
    expect(directive).toBeTruthy();
  });
});
