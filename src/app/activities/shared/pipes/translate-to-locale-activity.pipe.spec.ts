import { TranslateToLocalePipe } from './translate-to-locale.pipe';

describe('TranslateToLocalePipe', () => {
  it('create an instance', () => {
    //const pipe = new TranslateToLocalePipe();
   // expect(pipe).toBeTruthy();
  });
});
